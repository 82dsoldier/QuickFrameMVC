@model QuickFrame.Security.Areas.Security.Models.GroupListModel
@{
	Layout = "_PopupLayout";
}
<form asp-action="@Model.Action" asp-controller="@Model.Controller" asp-area="@Model.Area">
	<div class="col-md-6">
		<label for="GroupId"></label>
		<div>
	<select asp-for="GroupId"

			data-url="@Url.Action("GetRoles", "Groups", new { area = "Security", filter = Model.Filter })"
			data-id="id"
			data-text="name"></select>
		</div>
	</div>
	<div class="col-md-6">
		@Html.Partial("FormButtons")
	</div>
</form>
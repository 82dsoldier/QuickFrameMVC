@model GroupRoleIndexModel
@{
	ViewBag.Title = $"Groups in role {Model.RoleName}";
	ViewBag.UseSearch = true;
	ViewBag.UsePaging = true;
	Layout = "_Layout";
}
@section menu {
    @Html.Partial("MainMenu")
}

@section createbutton {
    <a asp-controller="Roles" asp-action="AddUserToRole" asp-route-id="@Model.RoleId" class="btn btn-sm btn-default fancybox" data-width="640px" data-height="480px" dc-after="mainCtl.reloadMe();"><i class="fa fa-plus"></i>Add user to role</a>
}
@{
	if(Model.GroupList.Any() == true) {
		<table class="table index table-striped table-bordered table-hover" style="border: 0;">
			<thead>
				<tr>
					<th style="width:5%;"></th>
					<th display-for="Name"></th>
				</tr>
			</thead>
			<tbody>
				@{
					foreach(var obj in Model.GroupList.OrderBy(u => u.GroupName)) {
						<tr>
							<td>
								<div class="dropdown">
									<a href="#" class="dropdown-toggle" style="display:inline-block;width:100%;" data-toggle="dropdown"><i class="fa fa-lg fa-ellipsis-v"></i></a>
									<ul class="dropdown-menu">
										<li><a asp-controller="Roles" asp-action="RemoveGroupFromRole" asp-route-id="@obj.GroupId" asp-route-roleId="@obj.RoleId" class="remove-object"><i class="fa-fw fa fa-times red"></i>Delete</a></li>
									</ul>
								</div>
							</td>
							<td display-for="@obj.GroupName"></td>
						</tr>
					}
				}
			</tbody>
		</table>
	}
}